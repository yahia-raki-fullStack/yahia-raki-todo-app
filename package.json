{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && gh-pages -d dist"
  },
  "dependencies": {
    "sortablejs": "^1.15.1",
    "vite": "^5.0.10"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
